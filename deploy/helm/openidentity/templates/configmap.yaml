apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "openidentity.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openidentity.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
